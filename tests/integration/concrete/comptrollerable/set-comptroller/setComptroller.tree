SetComptroller_Comptrollerable_Concrete_Test
├── when caller not current comptroller
│  └── it should revert
└── when caller current comptroller
   ├── when new comptroller without core interface id
   │  └── it should revert
   └── when new comptroller with core interface id
      ├── it should set the new comptroller
      └── it should emit a {SetComptroller} event
